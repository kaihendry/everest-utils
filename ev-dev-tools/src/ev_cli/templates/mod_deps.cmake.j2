list (APPEND MODULE_REQUIREMENTS
    {% for req in requires %}
    {{ req.type }}
    {% endfor %}
)

list (APPEND MODULE_IMPLEMENTATIONS
    {% for impl in implementations %}
    {{ impl.type }}
    {% endfor %}
)
